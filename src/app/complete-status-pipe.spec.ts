import { CompleteStatusPipe } from './complete-status-pipe';

describe('CompleteStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CompleteStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
